{
  "builds": [{ "src": "main.py", "use": "@vercel/python" }],
  "routes": [
    {
      "src": "/api/.*",
      "dest": "main.py",
      "config": {
        "api": {
          "timeout": 60 
        }
      }
    }
  ]
}
